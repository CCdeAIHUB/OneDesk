# Codex 稳定工程与防腐化开发 Skill

## 1. Skill 目标

本 Skill 用于约束 Codex / AI Agent 在长期软件项目中的代码生成、Bug 修复、重构、测试补充和需求迭代行为。

核心目标：

1. 提高代码生成稳定性；
2. 降低 AI 幻觉和自由发挥；
3. 防止临时 hotfix 腐化代码；
4. 保证代码高模块化、低耦合；
5. 将关键需求沉淀为可执行测试；
6. 让项目可以持续迭代，而不是越改越乱。

本 Skill 适用于所有正式项目代码修改，尤其适用于：

- 复杂业务系统；
- 插件系统；
- 跨平台项目；
- 桌面端 / 移动端 / 后端混合项目；
- API / Schema / IPC / 权限 / 生命周期较复杂的项目；
- 由 Codex 长期参与开发和维护的项目。

---

## 2. 使用触发条件

当用户提出以下任意需求时，必须启用本 Skill：

1. 新增功能；
2. 修改已有功能；
3. 修复 Bug；
4. 重构代码；
5. 调整架构；
6. 修改 API、Schema、IPC、错误码、插件协议；
7. 生成测试；
8. 让 Codex 继续开发已有项目；
9. 让 Codex 根据文档实现功能；
10. 让 Codex 分析代码问题或稳定性问题。

---

## 3. 总体原则

Codex 不允许直接进入编码状态。

任何代码修改前，必须先完成：

1. 阅读上下文；
2. 复述已理解规则；
3. 分析影响范围；
4. 制定改动计划；
5. 判断是否需要补测试；
6. 再开始修改代码。

所有代码必须遵守：

- 高模块化；
- 低耦合；
- 契约稳定；
- 测试优先；
- 最小变更；
- 可观测；
- 可回滚；
- 可维护；
- 关键逻辑有注释。

---

## 4. 上下文读取顺序

开始编码前，必须按顺序读取：

1. 项目 README；
2. 架构文档；
3. 当前任务相关 issue / spec / 需求说明；
4. 相关 ADR 架构决策记录；
5. 相关 JSON Schema / OpenAPI / 类型定义 / 协议文件；
6. 相关测试用例；
7. 相关模块源码；
8. 最近的错误日志、失败测试、复现步骤。

读取后必须先输出：

```md
## 已理解的上下文

- 本次任务目标：
- 已读取文件：
- 已发现的关键规则：
- 已有测试约束：
- 不允许破坏的接口 / Schema / 行为：
- 可能影响的模块：
- 需要进一步确认的问题：
```

未完成上下文阅读和规则复述前，禁止修改代码。

---

## 5. 编码前计划

正式编码前，必须输出改动计划。

格式如下：

```md
## 改动计划

### 任务目标
说明本次任务真正要解决什么问题。

### 涉及模块
列出将被修改或新增的模块。

### 模块职责
说明每个模块负责什么，不能混入什么职责。

### 不修改范围
明确哪些文件、模块、接口、行为不会被修改。

### 测试计划
说明需要新增、修改或运行哪些测试。

### 风险点
列出可能破坏的接口、状态、兼容性、权限、安全边界。

### 预计改动文件
列出预计修改或新增的文件。
```

如果任务较大，必须拆分阶段执行，禁止一次性大爆炸式生成。

---

## 6. 高模块化要求

所有代码必须按照职责拆分模块。

每个模块只能负责一类事情。

必须分离：

1. UI 展示；
2. 状态管理；
3. 业务逻辑；
4. 数据访问；
5. 平台能力调用；
6. API / IPC 适配；
7. 权限校验；
8. Schema 校验；
9. 错误处理；
10. 日志与可观测性。

禁止：

- 巨型文件；
- 巨型组件；
- 巨型 service；
- 万能 helper；
- 一个函数处理完整业务流程；
- 一个模块同时处理 UI、业务、协议、数据访问；
- 为了快速修复 Bug 把逻辑硬塞进现有文件。

如果单个文件出现以下情况，必须考虑拆分：

- 超过 300 到 500 行；
- 同时包含多个不相关职责；
- 出现大量 if/else 分支；
- 出现多个互不相关的 class / function；
- UI 与核心业务逻辑混在一起；
- 平台差异逻辑与业务逻辑混在一起。

拆分时必须保持公共接口稳定，避免无意义重构。

---

## 7. 低耦合要求

模块之间必须通过明确接口通信。

允许的边界形式包括：

- interface；
- service contract；
- schema；
- event；
- command；
- adapter；
- provider；
- gateway；
- repository；
- API client；
- domain service。

禁止：

- 跨层直接调用内部实现；
- UI 层直接访问底层平台 API；
- 业务层直接依赖具体平台实现；
- 使用全局可变状态传递复杂业务数据；
- 硬编码路径、环境、平台、权限；
- 通过临时 import 绕过模块边界；
- 为了修 Bug 直接访问其他模块私有字段。

平台差异必须封装在：

```md
adapter / provider / gateway / platform abstraction
```

业务逻辑必须依赖抽象接口，而不是依赖具体平台实现。

---

## 8. 关键逻辑注释要求

关键逻辑必须添加简洁注释。

注释必须解释：

- 为什么这样做；
- 业务规则是什么；
- 不能破坏的不变量是什么；
- 和哪些历史 Bug / 边界条件有关；
- 未来修改时需要注意什么。

必须添加注释的场景：

1. 权限判断；
2. 安全边界；
3. 插件加载；
4. 生命周期流转；
5. 状态机；
6. 并发 / 异步 / 锁；
7. fallback / retry / timeout / rollback；
8. 协议兼容；
9. 数据迁移；
10. 错误恢复；
11. 跨平台差异；
12. 复杂业务规则。

禁止：

- 用注释重复显而易见的代码；
- 用注释掩盖混乱实现；
- 注释和代码行为不一致；
- 添加临时分支但不说明原因；
- 留下无法追踪的 TODO。

---

## 9. Spec 必须尽量转成 TDD 测试

重要需求、业务规则、权限规则、协议规则不能只写在文档中。

凡是会影响系统行为的 spec，必须尽量转化为自动化测试。

测试用例内部必须包含简单场景描述注释。

示例：

```ts
// 场景：插件缺失 manifest 时，安装流程必须失败，不能进入权限申请阶段。
test("plugin install should fail when manifest is missing", async () => {
  const plugin = createPluginWithoutManifest()

  const result = await installPlugin(plugin)

  expect(result.success).toBe(false)
  expect(result.errorCode).toBe("PLUGIN_MANIFEST_MISSING")
})
```

测试必须表达：

1. 场景；
2. 输入；
3. 预期行为；
4. 错误码；
5. 边界条件；
6. 不允许发生的行为。

文档负责解释背景，测试负责锁定行为。

---

## 10. 测试保护规则

测试是需求事实的保护层，不能随意修改。

当测试失败时，必须先判断失败原因。

禁止为了让测试通过而直接修改测试。

只有以下情况允许修改测试：

1. 需求已经明确变更；
2. 测试与最新 spec 冲突；
3. 测试本身写错；
4. 测试描述不完整；
5. issue 明确要求调整验收标准。

修改测试前必须说明：

```md
## 测试修改说明

- 原测试验证的规则：
- 当前失败原因：
- 为什么原测试不再正确：
- 新测试验证的规则：
- 是否存在需求变更：
```

如果无法证明测试应该修改，必须优先修改实现，而不是修改测试。

---

## 11. Bug 修复流程

Bug 不能按临时 hotfix 处理。

所有 Bug 必须按 issue 形式进行分析。

修复流程：

1. 读取 Bug 描述；
2. 复现 Bug；
3. 判断 Bug 类型；
4. 编写失败测试；
5. 确认测试失败原因与 Bug 一致；
6. 分析根因；
7. 制定修复方案；
8. 修改实现；
9. 跑测试；
10. 检查是否引入临时兼容、降级、fallback；
11. 输出修复报告。

Bug 类型必须归类为以下之一：

- 需求缺失；
- 测试缺失；
- 实现错误；
- 架构漏洞；
- 状态机错误；
- 接口契约不一致；
- 并发 / 异步问题；
- 平台差异问题；
- 数据迁移问题；
- 权限 / 安全边界问题。

禁止：

- 只根据报错表面修复；
- catch 后忽略错误；
- 添加无依据 fallback；
- 添加不受测试保护的特殊分支；
- 为单个 case 添加破坏整体模型的 if/else；
- 不分析根因就直接修改代码。

---

## 12. 禁止临时 hotfix 腐化代码

Codex 不允许用短期补丁掩盖系统问题。

禁止以下行为：

```ts
try {
  riskyOperation()
} catch {
  // ignore
}
```

```ts
if (!value) {
  return defaultValue
}
```

```ts
if (unknownError) {
  return success
}
```

```ts
if (legacyCase || weirdCase || tempCase) {
  // 临时兼容
}
```

除非 fallback / retry / downgrade 是明确设计的一部分，否则不能添加。

如果必须添加兼容逻辑，必须同时满足：

1. 有 issue 说明原因；
2. 有测试覆盖；
3. 有注释说明边界；
4. 有明确错误码或日志；
5. 有未来移除条件；
6. 不破坏主流程设计。

---

## 13. 最小变更原则

每次任务必须采用最小必要改动。

禁止：

- 无需求地重构无关模块；
- 顺手改命名；
- 顺手调整格式；
- 顺手改目录结构；
- 为修一个 Bug 大面积改架构；
- 修改无关测试；
- 修改无关配置；
- 引入无关依赖；
- 把局部问题扩大成全局改造。

如果发现确实需要重构，必须先停止当前编码，并输出：

```md
## 需要重构的原因

- 当前任务为什么无法通过局部修改完成：
- 涉及哪些模块：
- 重构收益：
- 重构风险：
- 是否需要单独 issue：
- 是否需要用户确认：
```

---

## 14. 接口契约优先

凡是涉及以下内容，必须优先检查已有契约：

- Public API；
- JSON Schema；
- OpenAPI；
- IPC message；
- plugin manifest；
- error code；
- database schema；
- event name；
- command name；
- config format；
- file format；
- permission declaration。

禁止在未说明影响范围的情况下修改公共契约。

如必须修改契约，必须同步更新：

1. schema；
2. 类型定义；
3. 测试；
4. 文档；
5. 兼容策略；
6. 调用方代码；
7. 错误码；
8. 示例。

输出格式：

```md
## 契约变更说明

- 修改的契约：
- 修改原因：
- 影响的调用方：
- 是否兼容旧版本：
- 迁移方式：
- 已更新的测试：
- 已更新的文档：
```

---

## 15. 错误处理规范

所有关键失败路径必须使用统一错误结构。

错误对象至少包含：

```ts
type AppError = {
  errorCode: string
  message: string
  module: string
  recoverable: boolean
  cause?: unknown
  suggestion?: string
  traceId?: string
}
```

禁止：

- 抛字符串错误；
- 每个模块自定义一套错误格式；
- UI 直接显示底层错误；
- catch 后吞掉错误；
- 出错后返回成功；
- 权限失败后继续执行；
- Schema 校验失败后继续执行；
- 插件加载失败后静默跳过；
- IPC / 网络失败后无日志无错误码。

所有失败必须让调用方可感知。

---

## 16. 禁止沉默失败

以下行为默认禁止：

```ts
catch {}
```

```ts
catch (error) {
  console.log(error)
}
```

```ts
return null
```

```ts
return {}
```

```ts
return true
```

在关键路径中，失败必须包含：

1. 明确错误码；
2. 明确错误信息；
3. 所属模块；
4. 是否可恢复；
5. 日志；
6. 调用方可处理的失败结果。

关键路径包括：

- 权限申请；
- 插件导入；
- 插件安装；
- 平台兼容检测；
- 设备连接；
- API 请求；
- 数据写入；
- 文件读写；
- IPC 通讯；
- 状态迁移；
- 支付 / 账号 / 安全相关流程。

---

## 17. 状态机规则

涉及生命周期的功能必须显式定义状态机。

必须说明：

1. 有哪些状态；
2. 初始状态是什么；
3. 成功状态是什么；
4. 失败状态是什么；
5. 状态之间允许如何流转；
6. 哪些状态转换非法；
7. 失败后是否允许重试；
8. 是否允许取消；
9. 是否需要 rollback；
10. 是否需要持久化状态。

禁止使用多个松散 boolean 表达复杂状态。

禁止：

```ts
let isInstalling = true
let isLoaded = false
let hasError = true
let isReady = false
```

推荐：

```ts
type InstallState =
  | "pending"
  | "validating"
  | "requesting_permission"
  | "installing"
  | "installed"
  | "failed"
  | "rollback"
```

状态转换必须有测试覆盖。

---

## 18. 可观测性要求

关键路径必须具备可观测性。

关键路径至少需要：

1. 结构化日志；
2. 错误码；
3. 阶段耗时；
4. requestId / traceId；
5. 失败原因；
6. 关键输入摘要；
7. 关键输出摘要。

禁止日志中包含敏感信息，例如：

- token；
- password；
- private key；
- 用户隐私数据；
- 原始认证信息；
- 未脱敏的设备敏感信息。

日志必须帮助定位问题，而不是制造噪音。

---

## 19. 依赖管理规则

禁止未经说明新增第三方依赖。

如需新增依赖，必须说明：

```md
## 新增依赖说明

- 依赖名称：
- 用途：
- 为什么现有能力无法满足：
- 是否跨平台：
- 是否长期维护：
- 是否影响包体积：
- 是否有安全风险：
- 是否有更轻量替代方案：
```

禁止为了简单功能引入大型依赖。

禁止引入无人维护、来源不明、安全风险高的依赖。

---

## 20. ADR 架构决策记录

重大技术选择必须写入 ADR。

适合写 ADR 的内容：

- 协议选择；
- 插件格式；
- 权限模型；
- 跨平台架构；
- 数据存储方案；
- IPC 通讯方案；
- 状态机设计；
- 错误码体系；
- 测试策略；
- 安全模型；
- 架构边界。

ADR 必须包含：

```md
# ADR: 标题

## 背景

## 决策

## 原因

## 替代方案

## 影响范围

## 风险

## 未来演进
```

Codex 修改相关架构前，必须先读取已有 ADR。

禁止在未阅读 ADR 的情况下推翻已有架构决策。

---

## 21. 分阶段实现规则

复杂功能必须拆分为多个小阶段。

推荐顺序：

1. 定义 schema / interface；
2. 编写测试；
3. 实现核心逻辑；
4. 接入 API / IPC；
5. 接入 UI；
6. 补充错误处理；
7. 补充日志；
8. 补充文档；
9. 清理代码；
10. 跑完整测试。

禁止一次性同时完成：

- 架构；
- 数据层；
- 业务层；
- UI；
- 测试；
- 文档；
- 重构；
- 依赖调整。

每一阶段必须有明确可验证结果。

---

## 22. 完成后自检

每次代码修改完成后，必须输出自检报告。

格式：

```md
## 完成后自检报告

### 1. 本次完成内容
-

### 2. 修改文件
-

### 3. 新增 / 修改测试
-

### 4. 是否遵守高模块化
- 是 / 否
- 说明：

### 5. 是否保持低耦合
- 是 / 否
- 说明：

### 6. 是否修改公共接口 / Schema / 错误码
- 是 / 否
- 说明：

### 7. 是否存在 fallback / retry / catch
- 是 / 否
- 说明其必要性：

### 8. 是否存在沉默失败
- 是 / 否

### 9. 是否新增关键注释
- 是 / 否
- 说明：

### 10. 是否存在跨层调用
- 是 / 否

### 11. 是否有未覆盖边界情况
- 是 / 否
- 说明：

### 12. 已运行测试
-

### 13. 未运行测试及原因
-

### 14. 风险与后续建议
-
```

如果存在未运行测试，必须明确说明原因，不能假装已经验证。

---

## 23. Codex 输出格式要求

每次任务完成后，Codex 必须按照以下格式回复：

```md
# 任务完成报告

## 已读取上下文
-

## 已理解规则
-

## 改动摘要
-

## 模块设计
-

## 测试情况
-

## 错误处理
-

## 是否存在风险
-

## 后续建议
-
```

禁止只回复：

- Done；
- 已完成；
- Fixed；
- 应该可以了；
- 我修好了。

必须说明实际修改内容和验证结果。

---

## 24. 对 Codex 的强制禁令

Codex 在本 Skill 启用时，禁止以下行为：

1. 不读上下文直接改代码；
2. 不写计划直接编码；
3. 不分析根因直接修 Bug；
4. 为了通过测试修改测试；
5. 添加沉默失败；
6. 添加无依据 fallback；
7. 添加临时兼容但无注释、无测试、无 issue；
8. 修改公共接口但不更新 schema 和测试；
9. 引入新依赖但不说明原因；
10. 破坏模块边界；
11. 把业务逻辑写进 UI；
12. 把平台实现写进业务层；
13. 使用全局可变状态绕过架构；
14. 一次性大面积重构；
15. 顺手修改无关文件；
16. 用注释代替正确设计；
17. 留下不可追踪 TODO；
18. 把错误隐藏成成功；
19. 忽略权限、安全、生命周期边界；
20. 交付后不说明测试结果。

---

## 25. PowerShell 调用约束（项目强制规则）

本项目运行在 Windows 环境。系统自带的 Windows PowerShell 5.1（`powershell.exe`）存在以下问题：

- 默认编码为 GBK / UTF-16 LE 混合，`Get-Content` / `Set-Content` 在处理 UTF-8 文件时容易产生乱码；
- 部分模块（如 `Microsoft.PowerShell.Utility` 中的 `ConvertFrom-Json -AsHashtable`、`Invoke-WebRequest -SkipCertificateCheck` 等）在 v5.1 不可用；
- 跨平台脚本兼容性差，与项目其他平台（macOS / Linux 使用 `pwsh`）行为不一致。

因此项目内所有 AI Agent（Codex / Claude / Cursor / CodeBuddy 等）在调用 PowerShell 时必须遵守：

1. **必须使用 PowerShell 7+（`pwsh.exe`），禁止使用系统自带的 `powershell.exe`（v5.1）**。
   - 任何需要执行 PowerShell 语法的 shell 命令，都必须显式通过 `pwsh` / `pwsh.exe` 启动；不得直接依赖系统默认 shell。
2. 调用方式：
   - 在终端直接执行命令时，使用 `pwsh -Command "..."`；
   - 执行脚本文件时，使用 `pwsh -File script.ps1`；
   - 配置集成终端 / 任务 / CI 步骤时，显式指定 `pwsh` 而非 `powershell`。
3. 若当前环境未安装 PowerShell 7+，AI 必须先提示用户安装（`winget install Microsoft.PowerShell`），不得退回到 v5.1。
4. 在 PowerShell 7+ 中读取 / 写入文件时，显式指定 `-Encoding utf8` 或使用 `-AsByteStream`，避免依赖默认编码。
5. 在 PowerShell 7+ 中处理 JSON 时，使用 `ConvertFrom-Json -AsHashtable`（v5.1 不支持此参数，是判断是否使用 v7+ 的特征之一）。

禁止行为：

- 使用 `powershell.exe` 执行任何脚本或命令；
- 在 CI / 自动化 / 自动任务配置中使用 `powershell` 作为 shell；
- 假设系统默认 `powershell` 即为 PowerShell 7+（系统默认始终是 v5.1，PowerShell 7+ 的可执行文件名为 `pwsh`）。

本规则适用于项目内所有 AI Agent，不限于 Codex。

---

## 26. 桌面端组件重绘约束（项目强制规则）

WinUI 3 桌面端允许使用 WinUI 控件作为行为承载，但禁止直接交付 WinUI 原生默认视觉样式。

所有 AI Agent 修改桌面端 UI 时必须遵守：

1. 新增或修改按钮、输入框、密码框、数字输入框、下拉菜单、单选/分段选择、开关、列表、弹窗、滚动容器、标签页、卡片等组件时，必须按 ADBControl 当前整体 UI 风格重新绘制或套用项目统一样式辅助方法。
2. 新出现的组件类别必须同步补充统一样式规则，不能只在单个页面局部处理。
3. 组件必须同时检查深色模式和浅色模式配色，包括背景、边框、hover、pressed、selected、disabled、placeholder、弹出菜单和滚动条周边留白。
4. 禁止让 WinUI 默认蓝色按钮、默认灰色输入框、默认 TabView 标签、默认 ToggleSwitch、默认 RadioButton 等原生视觉直接出现在最终 UI。
5. 如果必须保留 WinUI 控件本身，应通过统一资源、模板或封装 helper 覆盖其视觉状态，并在验收中扫描确认没有遗漏。

---

## 27. 最终执行口令

当本 Skill 启用时，Codex 必须始终遵守以下执行顺序：

```md
1. 读取上下文
2. 复述约束
3. 分析影响范围
4. 输出改动计划
5. 判断是否需要测试
6. 先写或更新测试
7. 修改实现
8. 运行测试
9. 检查架构边界
10. 检查错误处理
11. 检查关键注释
12. 输出完成后自检报告
```

任何跳过以上步骤的行为，都视为不符合本 Skill。
